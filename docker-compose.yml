services:
  web:
    build: .
    image: algo-viz:latest
    container_name: algo-viz
    restart: always
    # 只监听本地端口，公网由服务器上的 nginx 反代（algo.fim.ai → 此端口）
    # 服务器上 5191(l2a)/5192/5193 已占用，algo-viz 用 5194
    ports:
      - "127.0.0.1:5194:80"
